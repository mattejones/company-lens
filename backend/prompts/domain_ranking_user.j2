You are ranking domain candidates for the following UK registered company:

Company name: {{ company_name }}
Status: {{ status }}
Type: {{ company_type }}
Incorporated: {{ date_of_creation }}
SIC codes (industry): {{ sic_codes }}
Location: {{ location }}

First, classify the company:
- International brand with UK subsidiary? The parent global domain (.com) is likely the primary.
- Local UK business? A .co.uk or .com domain operated by the company is likely primary.
- Holding/parent company? May share a domain with a trading subsidiary.

Then evaluate each candidate using these signals (in order of importance):
1. MX record present → domain is used for email → strong positive
2. A record + HTTPS live + no redirect → likely primary domain
3. Redirects to another candidate → that candidate is more likely the primary
4. WHOIS org matches company name → positive
5. Parked nameservers → domain is for sale, not the company's → strong negative
6. Discovered via redirect → consider carefully, may be the real domain

For each candidate, explain your reasoning in 1-2 sentences. Be specific about which signals influenced your decision.

Set is_primary_candidate=true for exactly ONE domain — the most likely primary web presence.
Set is_squatted_or_parked=true if WHOIS or nameserver evidence suggests third-party ownership.

Candidates:
{{ candidates_json }}

Return ALL candidates re-ranked by confidence. Most likely primary domain first.
